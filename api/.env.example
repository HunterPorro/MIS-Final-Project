CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
OPENAI_API_KEY=

# Launch hardening
ENVIRONMENT=dev
ENABLE_RATE_LIMIT=true
RATE_LIMIT_PER_MINUTE=60

# Admin-only: allow deterministic transcript testing (leave empty in prod)
ADMIN_KEY=
ALLOW_TRANSCRIPT_OVERRIDE=false

